---
name: Check Code Style

checkout_and_setup_python: &checkout_and_setup_python
jobs:
  lint:
    name: Pylint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set Up Python
        users: actions/setup-python@v1
        with:
          python-version: 3.8
      - name: Install dev tooling
        run: python -m pip install -r requirements/dev.txt
      - name: Install make
        run: sudo apt install -y -qq make
      - name: Run pylint
        run: make pylint

  black:
    name: Black
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set Up Python
        users: actions/setup-python@v1
        with:
          python-version: 3.8
      - name: Install dev tooling
        run: python -m pip install -r requirements/dev.txt
      - name: Install make
        run: sudo apt install -y -qq make
      - name: Check Black Formatting
        run: make black-check 

  pydocstyle:
    name: Pydocstyle
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set Up Python
        users: actions/setup-python@v1
        with:
          python-version: 3.8
      - name: Install dev tooling
        run: python -m pip install -r requirements/dev.txt
      - name: Install make
        run: sudo apt install -y -qq make
      - name: Check Pydocstyle
        run: make pydocstyle 

